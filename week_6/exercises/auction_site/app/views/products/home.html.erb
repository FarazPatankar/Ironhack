<% if user_signed_in? %>
	<%=link_to("Product List", products_path)%>
	<br>
	<%=link_to("Create Product", new_user_product_path(current_user))%>
	<br>
	<%=link_to("All My Products", user_products_path(current_user))%>
	<br>
	<%=link_to("All My Bids", user_bids_path(current_user))%>
	<br>
	<%=link_to("All your reviews", user_reviews_path(current_user))%>
	<br>
	<%=link_to("Sign Out", destroy_user_session_path, :method => :delete)%>
<% else %>
	<%=link_to("Sign Up", new_user_registration_path)%>
	<br>
	<%=link_to("Log In", new_user_session_path)%>
<% end %>